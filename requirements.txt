# Water-chemistry indices (LSI / RSI / pHs), extracted from this project into a
# standalone, dependency-free library: https://github.com/Madhvansh/cooling-tower-chem
# The physics engine uses it as the single source of truth when installed, and
# falls back to a vendored copy otherwise. Uncomment once it is on PyPI:
# cooling-tower-chem>=0.1.0

# Core
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# Deep Learning (MOMENT anomaly detection)
torch>=2.2
# torch==2.2.*+cu128 --index-url https://download.pytorch.org/whl/cu128  # Uncomment for CUDA

# API & Dashboard
fastapi>=0.100
uvicorn>=0.20

# Granger causality (cascade detection)
statsmodels>=0.14

# Testing
pytest>=7.0
httpx>=0.24

# Optional: AutonLab MOMENT pretrained loader
# momentfm>=0.1.0

# Optional: Online anomaly detection + drift detection
# river>=0.21

# Optional: SHAP explanations
# shap>=0.43
