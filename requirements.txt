# requirements.txt
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
# Torch: choose ONE of the following lines depending on your environment:
torch>=2.2         # CPU-only, PyPI default
# torch==2.2.*+cu128 --index-url https://download.pytorch.org/whl/cu128  # CUDA 12.1 (uncomment if needed)

# Optional: only if loading AutonLab/MOMENT pretrained models via MOMENTPretrainedLoader
momentfm>=0.1.0